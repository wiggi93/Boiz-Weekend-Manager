VITE_PB_URL=http://localhost:8090

# Web Push (HTPC only — generate with: npx web-push generate-vapid-keys)
# Backend container:  VAPID_PUBLIC_KEY, PUSH_SENDER_URL, PUSH_SENDER_TOKEN
# Push-sender container: VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, VAPID_SUBJECT, PUSH_SENDER_TOKEN
